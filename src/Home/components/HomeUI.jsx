import React from 'react'
import Button from '@material-ui/core/Button';

 const HomeUI = (props) => {

    /* const { getMovies } = props; */

    return (
        <div><h1>ESTOY EN VISTA HOME</h1>
            <Button>
                soy un boton
            </Button>
        </div>
    )
}

export default HomeUI;